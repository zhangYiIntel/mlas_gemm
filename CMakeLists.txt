cmake_minimum_required(VERSION 3.8)
project(ov_test)
add_compile_definitions(BUILD_MLAS_NO_ONNXRUNTIME)
add_subdirectory(mlas)
add_executable(mlas_test main.cpp)
target_link_libraries(mlas_test PRIVATE mlas)
target_include_directories(mlas_test PRIVATE mlas/inc)